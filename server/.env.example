# MongoDB Configuration Template
# Copy this file to .env and fill in your MongoDB credentials

# MongoDB Atlas Connection String
# Get this from MongoDB Atlas: Connect -> Connect your application
# Replace <username>, <password>, and <cluster> with your actual values
MONGODB_URI=mongodb+srv://<username>:<password>@<cluster>.mongodb.net/digistats?retryWrites=true&w=majority

# Server Port
PORT=5000

# Instructions:
# 1. Copy this file: cp .env.example .env
# 2. Replace <username> with your MongoDB username
# 3. Replace <password> with your MongoDB password
# 4. Replace <cluster> with your MongoDB cluster name
# 5. NEVER commit the .env file to Git!
